{
  "name": "ai-teaching-assistant-server",
  "type": "module",
  "version": "1.0.0",
  "description": "Server for the ai assistant bot. A school assignment, prg-8",
  "main": "server.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon --env-file=.env --watch server.js",
    "prod": "cross-env NODE_ENV=production nodemon --env-file=.env --watch server.js"
  },
  "author": "Faroeq",
  "license": "ISC",
  "dependencies": {
    "@langchain/core": "^0.1.18",
    "@langchain/openai": "^0.0.14",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "langchain": "^0.1.18",
    "nodemon": "^3.1.0"
  },
  "pnpm": {
    "overrides": {
      "@langchain/core": "^0.1.18"
    }
  },
  "keywords": [],
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
